global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "mcp_rust_benchmark"
    static_configs:
      - targets: ["host.docker.internal:9102"]
        labels:
          variant: "baseline"
      - targets: ["host.docker.internal:9103"]
        labels:
          variant: "structured"